import {
  StyleSheet,
} from 'react-native';

export const globalStyles = StyleSheet.create({
  marginLeftAuto: {
    marginLeft: 'auto',
  },
  flexDirectionRow: {
    flexDirection: 'row',
  },
  flexDirectionColumn: {
    flexDirection: 'column',
  },
});
